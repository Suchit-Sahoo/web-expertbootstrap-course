title: Web ExpertBootstrap
description: 'This is my bootstrap website '
template:
  repo: web-expertbootstrap-template
  name: web-expertbootstrap
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
